# Un manifest minimaliste pour déployer avec Cloud Foundry
# Il faut renommer le fichier manifest.yml avant de l'utiliser
---
applications:
# Le nom choisi doit être unique
- name: un-nom-unique
 # La ligne command correspond à la commande tapée normalement dans une console pour lancer l'application
 command: node app.js